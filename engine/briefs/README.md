# Named brief fixtures for engine:research --fixture.

Fixture providers return canned RFP-assistant payloads
(`lib/engine/providers/fixtures.ts`). Only add a brief here if the fixture
payloads match it. Research real ideas with `--brief path.json --live`.
