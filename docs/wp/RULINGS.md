# Rulings

Append-only owner/orchestrator decisions for questions not settled by project docs.

| Date | Scope | Question | Ruling | Decider |
|---|---|---|---|---|
| 2026-07-22 | MCP / Cloud Agents | Why can't Cloud Agents use Ideabrowser MCP? | Desktop/global MCP is not inherited. Wire Ideabrowser as Cloud Agent HTTP MCP at `https://www.ideabrowser.com/api/mcp/http` with `Authorization: Bearer ib_…` via cursor.com/agents MCP dropdown (and/or `IDEABROWSER_API_KEY` + `.cursor/mcp.json`). Never use `/api/mcp/sse` on Cloud Agents. | cloud-agent |
| 2026-07-22 | WP04 | Which 5 programmatic hubs next given full original inventory? | Publish audiences `freelancers`, `creators`, `small-business-owners` and problems `lead-generation`, `content-creation`. Highest SEO/AEO fit to Weekend MVP (AI weekend MVPs for indie builders) with enough taggable ideas; skip new tools (all AI builders covered) and new categories (already complete). | Orchestrator |
| 2026-07-22 | WP04 | Parallelize how? | Manifest tagging stays with orchestrator. Parallel workers: (1) audience route file only, (2) problem route file only. No worktrees — shared-file conflict avoided by file boundaries. | Orchestrator |
| 2026-07-25 | WP07 | About vs author URL model? | Split: `/about` (product) + `/john-iseghohi` (Person entity). Person `@id`/`url` = first-party author page; Cal only in sameAs + CTAs. | Owner |
| 2026-07-26 | WP10 | Apex vs www canonical host? | Canonical host is `https://www.weekendmvp.app`. Apex `weekendmvp.app` must 308 to www. Overrides cutover runbook note that said www→apex — live Vercel primary is already www. | Orchestrator |
| 2026-08-05 | WP13 | Who owns apex→www + path cleaning? | Middleware owns **one-hop** host+path canonicalization (apex→www, strip `.html`/trailing slash). Vercel Domains API apex `redirect` must stay `null` after WP13 ships — a host-only domain redirect preserves dirty paths and forces a second hop. Deploy middleware before clearing the domain redirect. | Orchestrator |
| 2026-08-05 | WP14 | Which GSC property for Sitemaps API? | Use URL-prefix `https://www.weekendmvp.app/` (not apex, not Domain) for Sitemaps API + service account. Keep Domain `sc-domain:weekendmvp.app` for whole-host analytics. SA: `g4-mcp@weekendmvp.iam.gserviceaccount.com` needs Full or Owner on the www prefix. | Orchestrator |
| 2026-08-05 | Platform program | Platform brand/domain: weekendmvp.app or new "beginmvp" brand? | Keep `weekendmvp.app`, no new brand. Platform marketing at `weekendmvp.app/build`, customer sites at `{project}.weekendmvp.app`. | Owner |
| 2026-08-05 | Platform program | Launch pricing (plan §5)? | Approved as proposed, including the free first repository-idea Validation Report as the activation hook. | Owner |
| 2026-08-05 | Platform program | Auth provider? | Convex Auth (magic-link email + Google OAuth) confirmed. | Owner |
| 2026-08-05 | Platform program | Ideabrowser plan upgrade vs own research pipeline? | Build our own Research Engine (plan §4.5). Upgrade rejected on cost: Pro $1,499/yr = 3 reports/mo (~$42/report, capped 36/yr), Empire $2,999/yr = 9/mo — cannot back a product that sells own-idea validation. Own pipeline ≈ $1.50–$4 marginal per report, unlimited, and becomes a core owned asset. Engine must replicate Ideabrowser's output *contract* (schema captured in plan §4.5) via independent research with our own citations — never copy their content. | Owner |
| 2026-08-05 | Platform program | Renew Ideabrowser at all, or full independence? | **Do not renew.** Expand the Research Engine into the full Idea Engine (plan §4.5.1): M1 signal ingestion, M2 idea generation + scoring into an owned `engine_ideas` library, M3 deep research, M4 content compilers for `/publish-idea` (Mode A2), `/publish-article`, `/publish-programmatic`, and `/newsletter`. Wave 4 (WP24–26) must land, pass per-compiler side-by-side quality gates ("exact or better"), and off-board the MCP before the subscription's renewal date. Publish the queued MCP backlog while the entitlement is active. Supersedes the "keep the existing plan for library reads" clause above. | Owner |
| 2026-08-05 | WP17 | Sitemap lastmod source + 0 indexed reading? | Idea `lastmod` comes from `ideas/manifest.json` `publishedAt` (idea MDX has no dates). Omit lastmod when unknown — never emit request-time “now”. Apex `/robots.txt` + `/sitemap.xml` must 308 to www. GSC Sitemaps “0 indexed” on a new www URL-prefix is lag/metric, not proof of a crawl block; use URL Inspection + Domain coverage. | Orchestrator |
| 2026-08-05 | Platform UX | How does the signed-in dashboard relate to the existing idea content? | Public `/ideas/{slug}` pages remain the sole canonical, shareable research pages. The signed-in Explore workspace uses the same idea records and adds recommendations, search/filtering, `Saved`, `Interested`, and project-derived `Building` state. Private platform pages are `noindex`; no second personalized research corpus is created. | Owner |
| 2026-08-05 | Platform UX | What is the primary repository-idea activation and dashboard model? | Use the approved Hilos-inspired dark editorial workspace: narrow product rail, contextual sidebar, artifact/status cards, and a persistent bounded-action composer. The repository-idea action promises `Preview this idea`; the flow is Explore → Research → Save/Interested → Preview → Customise → Publish → Project. | Owner |
| 2026-08-05 | Platform pricing | What is free on each acquisition path? | Supersedes the repository-report clause in the earlier pricing ruling: a repository idea gets an anonymous, expiring, watermarked landing-page preview free; signup keeps it and payment publishes it. A customer's own idea gets the first Validation Report free after signup and rate limiting. | Owner |
| 2026-08-05 | Platform program | Which work-package identifiers does the platform use? | Existing WP15-WP18 files are unrelated real work, so the Fable plan's proposed identifiers cannot be reused. The platform program starts at WP19; `docs/wp/program-manifest.md` owns the remapped sequence. | Orchestrator |
| 2026-08-05 | WP21 | Which service delivers magic-link email? | Use Resend. Keep the phishing-resistant confirmation interstitial; require an explicit verified sender via environment configuration and never fall back to a shared/demo sender for production. | Owner |
| 2026-08-05 | WP21 / platform auth | Must credential-backed Google OAuth block WP22 or development completion? | No. Resend magic links are the development authentication gate. Keep the Google provider code and deterministic security contracts, but defer Google credentials plus real redirect/callback/session/logout E2E to go-live activation. Do not claim Google passed; production remains blocked until that E2E and the normal backup, dry-run, secret, independent-review, and owner-approval gates pass. | Owner |
| 2026-08-05 | WP22 / data lifecycle | What deletion behavior is allowed in v1? | No destructive deletion in v1. User-owned platform records may be archived or soft-deleted and hidden from ordinary reads; credit-ledger and audit records remain immutable. Any physical deletion, retention expiry, or production backfill requires a later policy and separately gated migration. | Owner |
| 2026-08-05 | WP22 / authorization | Does v1 include a support or admin bypass for private customer data? | No. Private platform data is owner-only. Do not add a role, impersonation path, support bypass, or cross-owner read. A future operational-access design requires a separate scoped ruling, audit trail, and security gate. | Owner |
| 2026-08-05 | WP22 / documents | What document and upload boundary applies in v1? | No user file uploads in v1. Generated text/JSON documents are limited to 256 KiB per body; unbounded citations and steps use child tables, and large generated assets are referenced through Convex storage rather than embedded. Any customer-upload flow needs a later security/storage package. | Owner |
| 2026-08-05 | WP23 / recommendations | What powers the v1 `For you` Explore view? | Use a transparent deterministic rank over the canonical idea scores and recency, with a bounded category-affinity boost derived only from the signed-in user’s `Saved` and `Interested` state. Cold start uses canonical rank alone. No LLM, hidden profile, duplicate corpus, or client-trusted `Building` flag. | Orchestrator |
| 2026-08-05 | WP25 / briefs | How do draft edits, confirmation, and the optional Cal.com path work in v1? | Brief drafts are resumable and editable until confirmation. Confirmation freezes an immutable version; a later change creates the next draft revision and supersedes the prior version only after re-confirmation. Repository research snapshots remain immutable. Defer the Cal.com secondary path from v1. | Orchestrator |
| 2026-08-05 | WP23 / Building query | How is `Building` derived exactly after archived attempts? | Amend the frozen WP22 contract additively with `projects.by_ownerId_and_sourceIdeaId_and_archivedAt` over owner, source idea, and archive marker. WP23 must query the exact active range; an arbitrary window or unbounded filter is not acceptable. Gate the local schema amendment independently before integration; no deploy or data mutation is authorized. | Orchestrator |
| 2026-08-05 | WP24 / refund boundary | What refund and dispute behavior is allowed in the test-mode code package? | Full refunds and dispute-created reversals are exact-once. Partial refunds and dispute closed/funds-reinstated resolution are explicitly unsupported and must fail before mutation. They remain hard live-mode blockers until the owner approves proportional-credit, rounding, cumulative-money, status-transition, reinstatement, and idempotency policy in a separately gated schema/ledger extension. | Orchestrator; owner approval required before live mode |
| 2026-08-06 | WP26 | Which model provider synthesizes the Validation Report (scoring, Hormozi analysis, tags, summaries)? | OpenAI GPT — reuse the existing `OPENAI_API_KEY` already present in `.env.example`. This supersedes the draft plan's unstated Anthropic-leaning assumption in program-platform-plan.md §4.6 ("Ideabrowser + Anthropic + Stripe keys"); WP26 stories must name the exact model and cite it here if refined further. | Owner |
| 2026-08-06 | WP26 | Which search/community-signal source powers market stats and pain-evidence research, and what is the source-licensing stance? | Perplexity API, matching the plan's §4.5 draft. Citation-only sourcing: the engine may quote/cite and must store source URLs, but must never store or republish full third-party page content beyond what's needed for a citation snippet. | Owner |
| 2026-08-06 | WP26 | What per-report cost cap must hard-fail (and trigger the retry-once-then-refund policy) before a Validation Report run overspends? | $4.00 per report, matching the plan's ~$1.50-$4 marginal-cost estimate. WP26 stories must make this a configured, enforced ceiling (not just documentation) with telemetry on actual spend per report. | Owner |
| 2026-08-06 | WP26 | How long is cached research (citations, raw provider responses, generated report records) retained? | Keep indefinitely — the engine's output is an owned, compounding asset (`engine_ideas` library) that later powers `/publish-idea` Mode A2, `/publish-article`, `/publish-programmatic`, and `/newsletter` per plan §4.5.1. No retention-expiry job is required in WP26; a future privacy/PII ruling may still impose one before production activation, consistent with how WP22 deferred physical-deletion policy. | Owner |
| 2026-08-06 | WP26 / model pin | Which exact OpenAI model ID does synthesis/scoring use? (Closes the "OpenAI GPT names a family, not a model" gap in the earlier row.) | `gpt-5.6-sol` — the flagship tier, $5.00/1M input and $30.00/1M output as published on `developers.openai.com/api/docs/pricing` on 2026-08-06. Chosen on quality, not cost: a full run prices at roughly $0.52 (about $0.26 synthesis + $0.20 Perplexity + $0.02 DataForSEO + brief normalization), or about $1.04 if every step takes its one allowed retry — roughly 25% of the $4.00 cap at worst. Report quality is the product on the own-idea path, and the headroom is large enough that trading it for a cheaper tier is unjustified up front. `gpt-5.6-terra` ($2/$12) remains the documented downgrade if the S6 fixed-corpus eval shows no quality delta; record any such change as a new row rather than editing this one. **S2 must confirm the exact dated snapshot ID at implementation time** (OpenAI ships dated snapshots alongside floating aliases) and pin the snapshot, so model drift cannot silently change report quality or cost. | Owner |
| 2026-08-06 | WP26 / keyword data | Which provider supplies keyword volume, competition, and CPC for pipeline step 5? (Perplexity is ruled for market/community research only and is not a keyword-data source.) | DataForSEO, matching plan §4.5's own proposal. Pay-as-you-go with no subscription: $0.01 per task plus $0.0001 per returned item, about $0.02 per report. Returns real search volume, CPC, and competition for Google and Bing. Google Keyword Planner was rejected: it is free only with an active Google Ads spend, and without one it returns bucketed ranges ("1K-10K") that cannot satisfy the report's precision bar. An LLM may never synthesize, estimate, or infer these values — a hallucinated search volume is the exact failure mode this ruling exists to prevent, and a missing keyword provider must fail the step closed rather than fall back to a model guess. | Owner |
| 2026-08-06 | WP27 / anonymous preview storage | The pricing ruling grants an anonymous free preview with no signup, but every frozen WP22 table (`projects`, `site_configs`, `site_versions`) requires `ownerId: v.id("users")` and no capability table exists. Where does an anonymous preview artifact live? | Add one additive `preview_capabilities` table holding the token hash, source idea, `templateId`, the `SiteRenderSpec`, `expiresAt`, and an optional `claimedByUserId`. Signup claims it into a real owned project. Rejected relaxing `ownerId` to optional on `site_configs`/`site_versions`, which would weaken the owner-only authorization invariant on tables whose security gate already passed and force a re-review of every authz path; also rejected ephemeral no-persistence rendering, which contradicts "signup keeps it" and would make `/preview/{token}` meaningless. This amends the frozen WP22 contract additively only — no existing table, validator, or index changes — and follows the precedent of the WP23 index amendment. | Owner |
| 2026-08-06 | WP27 / capability lifetime | How long does an anonymous preview capability stay valid? | 7 days, reusable within the window since a visitor may reload or reopen the link. Expiry is enforced server-side on every capability read against a passed-in timestamp, never by client-side comparison and never dependent on a cleanup job having run. Rejected 24 hours (kills the realistic come-back-later conversion, which is where the free preview earns its value) and 30 days (weakens "expiring" as a security property while we host free artifacts per anonymous visitor). | Owner |
| 2026-08-06 | WP27 / template set | How many landing-page templates does v1 render? | Three, with a picker. Chosen against a recommendation of one; the stated trade-off is that three triples the renderer surface, XSS/schema test matrix, and accessibility review inside a 10-day launch window. That cost is accepted and must be paid explicitly: `WP27-S3` runs its injection and accessibility matrices **per template**, not once across the set. Template choice is stored in a WP27-owned `SiteRenderSpec` wrapper (`{contractVersion, templateId, siteInput}`) persisted as the site version's document body — it is a rendering property, not content, so it does not enter WP26-S1's frozen `SiteInputPayload` and needs no `templateId` column on the frozen `site_configs`. | Owner |
| 2026-08-06 | WP38 / super admin and editorial publishing | Who may access the operator control plane, Idea Engine, and canonical Weekend MVP content publishing workflows? | Add one server-verified `super_admin` role, initially bootstrapped to the owner's verified account through deployment-only configuration and then bound to the Convex Auth user ID; never commit or authorize from an email string in client code. Only `super_admin` may access engine signals/candidates, editorial review, compiler controls, canonical idea/article/programmatic/newsletter drafts, production activation, rollback, or engine configuration. Customers may invoke only bounded research and tenant-site workflows for projects they own. This supersedes only WP22's “no role” clause: there is still no generic cross-owner bypass, impersonation, arbitrary private-artifact browsing, direct ledger editing, or hard deletion. Every privileged action is explicit, server-authorized, reasoned, and immutably audited. | Owner |
