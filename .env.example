# Copy to .env.local for local Vercel dev if you need live Beehiiv calls.
# Vercel: set in project env (Production + Preview as needed).

# Beehiiv — subscribe routes + /newsletter stats lookups
BEEHIIV_API_KEY=
